#include <stdio.h>
#include "holberton.h"
/**
  * main - Program will print its own name.
  *
  * @argc: The amount of arguments(unused).
  * @argv: The argument vectors.
  *
  * Return: 0 when succesful.
  */
int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
